//! `rve` (Rust Video Editor) library modules.

/// Audio playback and streaming module.
pub mod audio;
/// FFmpeg process interaction and video metadata extraction.
pub mod ffmpeg;
/// Core application state models and configuration types.
pub mod model;
/// Terminal event handling and state update logic.
pub mod update;
/// Terminal UI rendering routines.
pub mod view;

